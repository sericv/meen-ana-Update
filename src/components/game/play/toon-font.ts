import { Baloo_Bhaijaan_2 } from "next/font/google";

/** Rounded, bubbly Arabic display font — gameplay screen only (cartoon casual identity). */
export const gpToonFont = Baloo_Bhaijaan_2({
  subsets: ["arabic", "latin"],
  weight: ["400", "500", "600", "700", "800"],
  variable: "--font-toon-gp",
  display: "swap",
});
